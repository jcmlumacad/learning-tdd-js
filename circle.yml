machine:
  node:
    version: 6.4.0
dependencies:
  override:
    - npm install
test:
  pre:
    - npm install -g karma jasmine bower
    - bower install
  override:
    - karma start --single-run